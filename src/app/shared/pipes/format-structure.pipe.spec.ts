import { FormatStructurePipe } from './format-structure.pipe';

describe('FormatStructurePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatStructurePipe();
    expect(pipe).toBeTruthy();
  });
});
